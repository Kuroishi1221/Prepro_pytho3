"""
test
"""
import math
def main():
    """omg"""
    can_rad = float(input())
    can_high = float(input())
    total_vol = 0
    total_vol += volume(can_rad, can_high)
    can_rad = float(input())
    can_high = float(input())
    total_vol = 0
    total_vol += volume(can_rad, can_high)
    can_rad = float(input())
    can_high = float(input())
    total_vol = 0
    total_vol += volume(can_rad, can_high)
    can_rad = float(input())
    can_high = float(input())
    total_vol = 0
    total_vol += volume(can_rad, can_high)
    can_rad = float(input())
    can_high = float(input())
    total_vol = 0
    total_vol += volume(can_rad, can_high)
    print("Total %.2f ml." %())

def volume(radius, height):
    can_vol = math.pi * radius **2

main()
